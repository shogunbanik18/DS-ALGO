#include<bits/stdc++.h>
using namespace std;

int reverse(int n)
{
    int sum=0;
    int temp=n;
    while(n>0)
    {
        int r=n%10;
        sum=(sum*10)+r;
        n=n/10;
    }
    return sum;
}

int main()
{
    int n;
    cin>>n;
    cout<<reverse(n)<<endl;
    return 0;
}
